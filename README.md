# gap-daily-charts starter
1) Upload all files at repo root; keep CSVs under `data/`.
2) Edit `.github/workflows/gap-auto-update.yml` as needed (cron time).
3) Click Actions → run workflow once to test.
